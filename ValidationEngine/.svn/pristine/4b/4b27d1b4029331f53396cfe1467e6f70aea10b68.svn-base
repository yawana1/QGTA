package validation;

import java.nio.file.Path;

import validation.Validator;

public class ValidationProcess {
	private Validator objValidator;
	
	public ValidationProcess(Path objInPreviousReportsDirectoryPath) {
		System.out.println("Generating a Validator object...");
		this.objValidator = new Validator(objInPreviousReportsDirectoryPath);
	}
	
	public void start() {
		this.objValidator.run();
	}
}
